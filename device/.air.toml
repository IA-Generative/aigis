root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/device-service ./cmd/server"
  bin = "./tmp/device-service"
  include_ext = ["go", "tpl", "tmpl", "html", "yaml", "yml", "json"]
  exclude_dir = ["tmp", "vendor", ".git"]
  delay = 500

[log]
  time = true
